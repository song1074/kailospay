# --- 공통 ---
node_modules/
dist/
build/
.vite/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# --- 프런트(루트)에 해당) ---
# (루트에도 node_modules, dist가 있으니 위에서 이미 제외됨)
# 환경변수(있다면)
.env
*.env

# --- 백엔드(server) ---
server/node_modules/
server/dist/
server/build/
server/uploads/          # 업로드 실제 파일 깃 제외
server/.env              # DB/API 키 등 비밀
server/*.env
server/*.log

# --- 인증서/민감파일 ---
certs/
*.pem
*.key
*.pfx
